# Violoop VLA11 kernel configuration and final board policy.

# USB gadget: HID, CDC ACM and FunctionFS.
CONFIG_USB_CONFIGFS=y
CONFIG_USB_CONFIGFS_UEVENT=y
CONFIG_USB_CONFIGFS_ACM=y
CONFIG_USB_CONFIGFS_F_HID=y
CONFIG_USB_CONFIGFS_F_FS=y
CONFIG_PHY_ROCKCHIP_USBDP=y

# VLA11 supports both populated screen assemblies. U-Boot selects the matching
# touch controller from LCD-ID; both factory-programmed panels use the same
# generic panel-simple DSI path without an initialization command sequence.
CONFIG_DRM_PANEL_SIMPLE=y
# Compile the opt-in VP1 X/Y hardware mirror path only for VLA11.  Without
# this symbol the shared VOP2 driver follows the original Rockchip logic.
CONFIG_ROCKCHIP_VOP2_DT_MIRROR=y
# CONFIG_DRM_PANEL_FPT_TTCM03921235 is not set
CONFIG_DRM_PANEL_JUJING_JN3929595A=y
CONFIG_ROCKCHIP_PANEL_NOTIFIER=y
CONFIG_TOUCHSCREEN_FT3519=y
CONFIG_TOUCHSCREEN_HYN_CST3640=y
# CONFIG_TOUCHSCREEN_SW42000 is not set
CONFIG_LEDS_TRIGGERS=y
CONFIG_LEDS_TRIGGER_HEARTBEAT=y

# LT7911EXC Type-C/DP to CSI3 bridge.
CONFIG_VIDEO_LT7911EXC=y

# KM8V7001JM-B810 uses the RK3576 UFS host (UFS 2.1, two lanes).
CONFIG_SCSI_UFSHCD=y
CONFIG_SCSI_UFSHCD_PLATFORM=y
CONFIG_SCSI_UFS_ROCKCHIP=y

# SAI1 uses the normal DMA PCM path; software digital loopback is disabled.
# CONFIG_SND_SOC_ROCKCHIP_DLP_PCM is not set

# VLA11 is populated with ES8390. ES8390 uses the upstream ES8389 driver.
CONFIG_SND_SOC_ES8389=y

# Keep the product ALSA contract deterministic: the built-in ES8390
# multicodec card registers first as card0; load the PDM simple-card after
# userspace starts so it becomes card1.  The existing product application
# expects playback on hw:0,0 and PDM capture on plughw:1,0.
CONFIG_SND_SIMPLE_CARD=m

# PWM1_CH5_M1 is owned directly by violoop-thermald through PWM sysfs.
# CONFIG_SENSORS_PWM_FAN is not set

# RK1828 is attached to the VLA11 PCIe0 root complex. The endpoint function
# driver is built from the versioned standalone RK182x source in the VLA11
# rootfs component, so the shared in-kernel demo driver stays disabled.
CONFIG_PCI=y
CONFIG_PCIEPORTBUS=y
CONFIG_PCI_MSI=y
CONFIG_PCIE_DW_ROCKCHIP=y
CONFIG_PHY_ROCKCHIP_NANENG_COMBO_PHY=y
CONFIG_DMABUF_HEAPS=y
CONFIG_DMABUF_HEAPS_SYSTEM=y

# Keep the complete RK3576 host-side support set used by the validated
# PCIe1 baseline.  These also support the VLA11 thermal/devfreq policy.
CONFIG_ROCKCHIP_IPA=y
CONFIG_ROCKCHIP_OPP=y
CONFIG_ROCKCHIP_SYSTEM_MONITOR=y
# EP（设备侧）驱动保持关闭——RK3576 在本板是 RC/主机，不当端点。
# CONFIG_PCIE_DW_ROCKCHIP_EP is not set

# RKEP 是**主机侧**驱动（Kconfig 原文 "host side function driver"），方向与上面那条
# 相反：它正是用来驱动 PCIe 上那张 RK1828 算力卡的。ODM 的板级提交把三项当成一组
# 关掉了，算力卡因此完全没人管（lspci 见 1d87:182a，BAR 全 [disabled]、无驱动绑定）。
#
# 建成模块（=m）而非内建：算力卡是**选配**硬件。模块随镜像出厂但不在内核里，
# 卡在位时由 udev 按 PCI modalias 自动加载，不在位则完全惰性。
# （modalias 依赖我们对 pcie-rkep.c 的 MODULE_DEVICE_TABLE 修正。）
CONFIG_PCIE_FUNC_RKEP=m
# PCIE_FUNC_RKEP 的 Kconfig 里 select 它；显式写出以免再被当成无关调试项关掉。
CONFIG_PCIE_DW_DMATEST=y
